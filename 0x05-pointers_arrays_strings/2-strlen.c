#include "main.h"
#include <stdio.h>
/**
 * _strlen - Generate length of a string
 * Return: string length
 * @s: Collects string
 */
int _strlen(char *s)
{
	char string[] = *s;
	int a;
	for (i = 0; i <= string; i++)
	{
		a += i;
	}
	return (a);
}

