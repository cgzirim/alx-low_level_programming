#include <string.h>
#include <stdio.h>
#include "main.h"
/**
 * _strcpy - copies sting to another location
 * @dest: receives string
 * @src: copied from
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
