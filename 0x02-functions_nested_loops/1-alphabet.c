#include <stdio.h>
/**
 * main - Output alphabet
 * print_alphabet - Outputs alphabet to main
 * Return: 0
 */
void print_alphabet(void);
int main(void)
{
void print_alphabet()
{
char = 'a';
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar('\n');
return;
}
return (0);
}
