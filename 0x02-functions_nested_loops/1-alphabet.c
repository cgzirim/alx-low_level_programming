#include <stdio.h>
/**
 * main - Output alphabet
 * print_alphabet - Outputs alphabet to main
 * Return: 0
 */
void print_alphabet(void)
{
char = 'a';
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar('\n');
return (0);
}
int main(void)
{
print_alphabet(void);
}
return (0);
}
