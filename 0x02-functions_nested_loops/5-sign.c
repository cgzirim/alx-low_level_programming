#include "main.h"
/**
 * print_sign - Output based on number sign
 * Return: 1 if number is positive, -1 if negative * and 0 if zero
 */ 
int print_sign(int n)
{
if (n > 0)
return (1)
putchar(1 +'0');
else if (n < 0)
return (-1)
putchar('_');
else
return (0);
}
