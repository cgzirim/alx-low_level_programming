#include "main.h"
/**
 * mul - Outputs product of two numbers
 * @a: collects an integer
 * @b: Recieves an integer
 * Return: value of product
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
